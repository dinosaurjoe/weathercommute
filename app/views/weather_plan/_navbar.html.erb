<div class="navbar container grid">
  <div class="navitem navfirst">
    <p>
      Now: <%= @temperature_summary[:temperature_now] %> C° /
      Low: <%= @temperature_summary[:low] %> C° / 
      High: <%= @temperature_summary[:high] %> C°
     </p>
  </div>
  <div class="navitem">
    <%= icon('fas', 'cloud') %>
  </div>
  <div class="navitem">
     <p> Current Time: <%= Time.now.localtime.strftime('%H:%M') %> </p>
  </div>
  <div class="navitem">
     <p> Hamburg, DE </p>
  </div>
</div>